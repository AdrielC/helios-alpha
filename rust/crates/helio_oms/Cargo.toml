[package]
name = "helio_oms"
version = "0.1.0"
edition = "2021"
description = "Portable event-sourced order management contracts and FIX 4.4 translation for Helios Alpha"

[dependencies]
helio_execution = { path = "../helio_execution" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
