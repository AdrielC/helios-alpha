[package]
name = "helio_execution"
version = "0.1.0"
edition = "2021"
description = "Fail-closed risk, cost, broker, and capital-admission controls for Helios Alpha"

[dependencies]
helio_scan = { path = "../helio_scan" }
helio_time = { path = "../helio_time" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
