# Initial findings

This file is intentionally empty at bootstrap time.

After your first serious sample:

- State the universe, date range, and SSI version (commit hash + config snapshot).
- Report pre-registration style: which horizons, which tickers, which control definition.
- Paste a small table: effect size, bootstrap p-value, and whether it survives a clustered placebo (shuffle dates).
